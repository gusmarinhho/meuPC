{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Chave secreta para assinatura de tokens JWT (autenticação). Gerada automaticamente para desenvolvimento; substitua por uma chave real em produção.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
